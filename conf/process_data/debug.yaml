defaults:
  - process_data_config
  - _self_

data_config: {
  total_ratio: 0.005,
  validation_ratio: 0.1,
  test_ratio: 0.05,
  max_length_text: 1024,
  max_length: 256,
  data_loader_batch_size: 4
}